import { DoubleDigitsPipe } from './double-digits.pipe';

describe('DoubleDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
